{{!--<article class="gh-card {{post_class}}">
    <a class="gh-card-link" href="{{url}}">
        {{#if feature_image}}
        <div class="gh-card-image">
            <img
                srcset="{{img_url feature_image size="s"}} 300w,
                        {{img_url feature_image size="m"}} 600w,
                        {{img_url feature_image size="l"}} 1000w,
                        {{img_url feature_image size="xl"}} 2000w"
                sizes="(max-width: 1000px) 400px, 800px"
                src="{{img_url feature_image size="m"}}"
                alt="{{title}}"
                {{#if @custom.show_images_in_feed}}loading="lazy"{{/if}}
            >
        </div>
        {{/if}}
        <div class="gh-card-wrapper">
            <h2 class="gh-card-title is-title">{{title}}</h2>
            {{#if excerpt}}
            <p class="gh-card-excerpt is-body">{{excerpt}}</p>
            {{/if}}
        </div>
    </a>
</article>--}}

<article class="page-card portfolio-link">
    <a href="{{url}}" class="page-card-link">
        <h2 class="page-card-title">{{title}}</h2>
    </a>
</article>